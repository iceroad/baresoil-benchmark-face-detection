const Aggregates = require('./Aggregates'),
  ClientHealth = require('./ClientHealth'),
  RequestStats = require('./RequestStats')
  ;

module.exports = {
  Aggregates,
  ClientHealth,
  RequestStats,
};
